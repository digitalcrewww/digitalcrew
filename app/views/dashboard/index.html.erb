<div>
  <% if @airline&.logo.attached? %>
    <%= image_tag @airline.logo, alt: "Airline Logo", class: "airline-logo" %>
  <% end %>
</div>

<h2>Dashboard</h2>

<h3>Welcome, <%= @user.username %>!</h3>

<p>Airline Callsign: <%= @airline.callsign %></p>
